---
interface Props {
	summary: string;
	cmsPath?: string;
}

const { summary, cmsPath } = Astro.props;
---

<details class="phone-collapse">
	<summary class="phone-collapse__summary" data-cms={cmsPath}>{summary}</summary>
	<div class="phone-collapse__content">
		<slot />
	</div>
</details>
